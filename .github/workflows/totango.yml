name: totango

on:
  issues:
    types: [opened]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: jmassardo/issues-to-totango@v1.6
      with:
        ACCOUNT_ID: ${{ secrets.ACCOUNT_ID }}
        APP_TOKEN: ${{ secrets.APP_TOKEN }}
        ACTIVITY_TYPE: "adoption"
        TOUCHPOINT_REASON: "e53621bc-f66a-49f0-a886-537b5c64df22"
