name: gh_cli
on:
  issues:
    types: [opened]
env:
  GITHUB_TOKEN: ${secrets.PROJECTS_SECRET}

jobs:
  gh-cli:
    runs-on: ubuntu-latest
    env:
      ISSUE_URL: ${{ github.event.issue.html_url }}
    steps:
      - name: Run a one-line script
        run: gh issue view $ISSUE_URL
