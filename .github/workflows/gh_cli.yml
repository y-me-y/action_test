name: gh_cli
on:
  workflow_dispatch:

jobs:
  gh-cli:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${secrets.PROJECTS_SECRET}
    steps:
      - name: Run a one-line script
        run: gh issue list
